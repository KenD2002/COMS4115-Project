#include <stdio.h>
#include <string.h>

int main() {
    int x = 0;
    if ((x > 20)) {
        printf("%s\n", "x is greater than 20");
    }
    else {
        printf("%s\n", "x is less than 20");
    }
    if ((x < 20)) {
        printf("%s\n", "x is less than 20");
    }
    else {
        printf("%s\n", "x is greater than 20");
    }
    return 0;
}
