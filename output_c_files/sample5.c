#include <stdio.h>
#include <string.h>

int main() {
    int x = 0;
    while ((x < 5)) {
        printf("%s\n", "x is: ");
        printf("%d\n", x);
        int y = -2;
        x = (x + 1);
    }
    while (1) {
        return 0;
    }
    return 0;
}
